name: catalog_test_flutter_app_subchart
description: "Onboard-detect fixture: Flutter app at root that also ships a helm sub-chart."
publish_to: 'none'
version: 0.0.0+0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
