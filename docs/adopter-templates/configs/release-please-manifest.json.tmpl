{
{{- $first := true }}
{{- range .profile.components }}
{{- if not $first }},{{ end }}
{{- $first = false }}
  "{{ .path }}": "{{ if index . "version" }}{{ .version }}{{ else }}{{ $.profile.current_version }}{{ end }}"
{{- end }}
}
