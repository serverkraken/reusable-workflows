CLAUDE.md
CLAUDE-*.md
GEMINI.md
GEMINI-*.md
.claude/

# Scratch review/spec docs that live untracked at the repo root
REVIEW-*.md

# Tooling
*.tmp
.act/
.gocache/

# Provider-Cache aus `tofu init` in den tofu-Fixtures. Die .terraform.lock.hcl
# daneben ist dagegen GETRACKT — sie ist der Grund, warum
# `init -lockfile=readonly` in der self-CI durchlaeuft.
.terraform/

# Coverage
coverage/
*.bashcov
coverage.out

# Local Go binaries
bin/

# Release-Assets aus scripts/build-sk-workflows-assets.sh
dist/

# OS
.DS_Store
.coverage
__pycache__/
*.pyc
